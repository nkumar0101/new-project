#!/bin/bash
# App is deployed on Vercel — no local service startup needed.
# Output the base URL so testbot knows where to run tests against.
echo "https://new-project-git-nk-testbot-nkumar0101s-projects.vercel.app"
